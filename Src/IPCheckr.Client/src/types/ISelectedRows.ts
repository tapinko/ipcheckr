export interface IClassesSelectedRows {
  rowId: number[]
  classId: number[]
  className: string[]
}

export interface IUsersSelectedRows {
  rowId: number[]
  userId: number[]
  username: string[]
}